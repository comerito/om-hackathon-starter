// Cross-module entity links — deferred until ParticipantProfile is actively queried cross-module
// The link between ParticipantProfile and CustomerUser is managed via FK (customerUserId) directly
export const extensions: never[] = []
