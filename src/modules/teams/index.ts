import './commands/teams'
import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'teams',
  title: 'Teams',
  version: '0.1.0',
  description: 'Team formation, membership, and invitation lifecycle',
}
