import './commands/judging'
import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'judging',
  title: 'Demos & Judging',
  version: '0.1.0',
  description: 'Judge panels, scoring criteria, demo sessions, and presentation timer',
}
